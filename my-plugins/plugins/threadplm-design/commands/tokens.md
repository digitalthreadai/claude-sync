---
description: "Build design tokens (primitive, semantic, component), CSS variables, spacing and typography scales, and component specs"
---

Invoke the `design-system` skill via the Skill tool.

Target or request: $ARGUMENTS

Follow the skill's SKILL.md end-to-end. If $ARGUMENTS is empty, ask the user for whether the user wants a full token architecture, a specific component spec, or a CSS variable scale.

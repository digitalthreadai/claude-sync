---
description: "Build accessible UIs with shadcn/ui and Tailwind - components, responsive layouts, dark mode, design system consistent styling"
---

Invoke the `ui-styling` skill via the Skill tool.

Target or request: $ARGUMENTS

Follow the skill's SKILL.md end-to-end. If $ARGUMENTS is empty, ask the user for which component or layout to build and in which project.

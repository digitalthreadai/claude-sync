---
description: "Turn a codebase into an interactive single-page HTML course (local folder, GitHub URL, or current project)"
---

Invoke the `codebase-to-course` skill via the Skill tool.

Target or request: $ARGUMENTS

Follow the skill's SKILL.md end-to-end. If $ARGUMENTS is empty, ask the user for target codebase path or GitHub URL (empty means 'use current working directory').
